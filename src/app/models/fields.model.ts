export interface Fields {
    name: string;
    route: string;
    instruction: string;
    menuOptions: MenuOptionsItem[];
}
export interface MenuOptionsItem {
    name: string;
    route: string;
}
